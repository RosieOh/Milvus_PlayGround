# DECISIONS

선택의 **근거**와 **막힌 지점**을 남긴다. 성공 기록보다 실패 기록이 신뢰를 준다.
새 항목은 위가 아니라 아래에 추가한다 — 번호가 곧 시간 순서다.

형식: `D-NNN · 제목` / 맥락 / 결정 / 근거 / 대안과 기각 사유 / 재검토 조건

---

## D-001 · 데이터셋은 MIRACL Korean

- **맥락** — 벤치마크의 신뢰도는 데이터 분포에서 나온다. 랜덤 벡터로 측정한
  recall·QPS는 아무것도 말해주지 않는다.
- **결정** — `miracl/miracl-corpus` (v1.0-ko) 를 코퍼스로, `miracl/miracl` (ko) 의
  qrels 를 정답 라벨로 사용한다.
- **근거**
  - 한국어 위키 기반 약 1.4 M passage — 계획의 1.2 M 타깃과 거의 일치
  - 압축 226 MB (3 shard) 로 다운로드 부담이 없다
  - **사람이 라벨링한 qrels** 가 함께 제공된다
  - gated 아님, 공개 라이선스 → 누구나 재현 가능
- **효과** — 기획서 09의 리스크 "골든셋 품질 미달 → LLM 라벨링 + 수동 검수"가
  사라졌다. 사람 판정을 그대로 쓴다.
- **대안과 기각 사유**
  - *KorQuAD* — 문서 수가 1만 단위. 규모가 부족하다.
  - *mMARCO ko* — 8.8 M passage. 기계 번역이라 한국어 자연스러움이 떨어진다.
  - *한국어 위키 원본 덤프* — 청킹을 직접 해야 하고 정답 라벨이 없다.
- **재검토 조건** — 도메인 특화 코퍼스를 추가하고 싶어질 때.

---

## D-002 · Python 3.12 고정 (uv)

- **맥락** — 시스템 기본은 3.14.7. `milvus-lite` 는 `py3-none-any` 휠이라 설치는
  되지만, `torch` / `sentence-transformers` 계열 휠이 3.14 에서 아직 불안정하다.
- **결정** — `uv venv --python 3.12` 로 고정. `.python-version` 에 명시.
- **근거** — 환경 문제로 하루를 태우는 것이 이 프로젝트에서 가장 비싼 실패다.
  버전을 파일로 못 박아 재현 경로에서 제거한다.
- **비고** — uv 로 3.12.14 가 이미 설치되어 있어 추가 설치 비용은 없었다.
- **재검토 조건** — torch 가 3.13/3.14 휠을 안정적으로 내놓을 때.

---

## D-003 · Milvus 3.0.1 Standalone · 공식 compose 를 그대로 vendoring

- **결정** — `v3.0.1` 릴리스의 `milvus-standalone-docker-compose.yml` 을 수정 없이
  저장소에 포함한다. 손댈 일이 생기면 이 문서에 기록한 뒤에 손댄다.
- **근거**
  - 직접 작성한 compose 는 업스트림과 어긋나는 순간 디버깅 비용이 폭증한다
  - 3.0 은 `MQ_TYPE=woodpecker` 로 Pulsar/Kafka 없이 뜬다 — 컨테이너 3개로 끝
  - K8s(Distributed) 는 기획서 02 의 비목표
- **구성** — etcd(메타) + MinIO(오브젝트) + standalone. 포트 19530(gRPC) /
  9091(healthz·metrics) / 9001(MinIO 콘솔).

---

## D-004 · 프로젝트를 외장 APFS 볼륨으로 이전

- **맥락** — 내장 디스크 여유가 14 GB (93 % 사용) 였다. 1.4 M × 768 dim 임베딩과
  Milvus 인덱스·MinIO 데이터를 담을 수 없었다.
- **결정** — 저장소 전체를 `/Volumes/satechi/study/Milvus_PlayGround` 로 이전.
  Docker 볼륨도 compose 기본값(`${DOCKER_VOLUME_DIRECTORY:-.}`)에 따라 같은 볼륨에 둔다.
- **측정값** (1 GB `dd`, 순차)

  | 위치 | write | read |
  |---|---|---|
  | 외장 satechi (APFS) | 934 MB/s | 945 MB/s |
  | 내장 SSD | 749 MB/s | 2,004 MB/s |

  순차 쓰기는 오히려 외장이 빠르고, 읽기만 약 2.1배 느리다. 벤치 수행에 충분하다.
- **남은 리스크 — 반드시 README 에 공개할 것**
  1. `dd` 는 **순차** I/O 만 잰다. DiskANN·mmap 은 **랜덤** 읽기가 지배적이므로
     이 수치로 갈음할 수 없다. 컨테이너 안에서 랜덤 I/O 를 따로 측정한다. *(TODO)*
  2. Docker Desktop 의 VirtioFS 바인드 마운트 오버헤드가 위 수치 위에 얹힌다.
     호스트 수치가 곧 Milvus 가 보는 수치는 아니다.
  3. 볼륨이 분리되면 etcd/MinIO 데이터가 손상될 수 있다. 분리 전 `make down` 필수.
- **재검토 조건** — 디스크 I/O 가 벤치 결과를 지배하는 정황이 보이면, 저장 계층만
  내장 디스크의 Docker named volume 으로 옮기고 재측정한다.

---

## D-005 · 임베딩은 e5-small(384d) → e5-base(768d)

- **결정** — v1 은 `intfloat/multilingual-e5-small` (384 dim),
  v2 는 `intfloat/multilingual-e5-base` (768 dim).
- **근거**
  - **차원이 달라야 한다.** 재색인의 난이도는 차원 변화에서 온다 — 인덱스 재빌드가
    강제되고 메모리 프로파일이 바뀐다. 같은 차원끼리 바꾸면 문제가 절반으로 준다.
  - small 은 베이스라인 구축이 빠르다. W1·W2 반복 속도가 곧 완주 확률이다.
  - e5 계열은 `query: ` / `passage: ` 접두사 규약이 있다. 이걸 빠뜨리면 성능이
    조용히 떨어진다 — 설정에 명시해 사고를 막는다.
- **스트레치** — `BAAI/bge-m3` (1024 dim) 를 v2 후보로 추가. 한국어 성능이 더
  높아 "게이트 통과" 사례를 만들기 좋다. W3 에서 실측 후 결정한다. *(미결)*

---

## D-006 · 코퍼스 티어 분리

- **결정** — `dev` 50 K / `main` 500 K / `full` 전량(~1.4 M). 기본은 `dev`.
- **근거** — 제약은 더 이상 디스크가 아니라 **임베딩 시간**이다. 스윕을 20회 돌리는
  동안 전량을 쓰면 반복이 불가능해진다. dev 로 개발하고, 최종 수치만 main/full 로 낸다.
- **규칙** — 리포트에 실리는 모든 수치는 **티어를 함께 표기**한다. 티어가 다른 수치를
  같은 표에 넣지 않는다.

---

## D-007 · recall 을 두 종류로 분리해서 측정한다

흔한 혼동이고, 섞으면 게이트 판정이 무의미해진다.

| 지표 | 정답 기준 | 측정 대상 | 사용처 |
|---|---|---|---|
| **ANN recall@10** | 같은 임베딩의 FLAT 브루트포스 | **인덱스** 품질 | M1 스윕 |
| **nDCG@10 · Recall@10** | MIRACL qrels (사람 라벨) | **임베딩 모델** 품질 | M2 게이트 |

- 인덱스 파라미터를 바꿀 때 움직여야 하는 것은 **ANN recall** 이다. qrels 기준
  점수는 거의 안 움직인다 — 모델이 그대로이기 때문이다.
- 임베딩 모델을 바꿀 때 판정해야 하는 것은 **qrels 기준 점수**다. ANN recall 은
  두 모델 각각의 FLAT 대비로 계산되므로 모델 간 비교에 쓸 수 없다.
- 기획서 03 의 "recall@10 회귀 −0.01 초과 시 중단" 은 **qrels 기준**을 말한다.

---

---

## D-008 · MinIO 이미지를 quay.io 로 교체 (공식 compose 패치)

- **증상** — `make up` 이 이미지 pull 단계에서 실패.

  ```
  Error response from daemon: pull access denied for minio/minio,
  repository does not exist or may require 'docker login'
  ```

- **오진 가능성** — 메시지가 `docker login` 을 언급해서 Docker Hub 인증 문제로 읽히지만,
  아니었다. 레지스트리를 직접 조회해 확인했다.

  | 레지스트리 | `minio/minio` | 태그 `RELEASE.2024-12-18T13-15-44Z` |
  |---|---|---|
  | Docker Hub | **object not found** | — |
  | quay.io | public | 존재 |

  MinIO 가 Docker Hub 배포를 내렸고, **Milvus 3.0.1 의 공식 standalone compose 가
  아직 옛 경로를 가리키고 있다.** 즉 이 파일을 그대로 쓰면 지금은 아무도 띄울 수 없다.

- **결정** — `minio/minio` → `quay.io/minio/minio` 로 교체. 태그는 동일하게 유지한다.
  D-003 의 "무수정 vendoring" 원칙에 대한 **첫 예외**이며, compose 파일 안에도
  `# PATCHED (D-008)` 주석을 남겼다.
- **대안과 기각 사유**
  - *더 최신 MinIO 태그로 올리기* — 업스트림이 검증한 조합에서 벗어난다. 변수를 하나만 바꾼다.
  - *MinIO 대신 다른 S3 호환 스토리지* — 범위를 벗어나고, Milvus 가 검증한 구성이 아니다.
  - *`docker login` 시도* — 원인이 인증이 아니므로 해결되지 않는다.
- **후속** — Milvus 저장소에 이슈로 보고할 것. 기획서가 보너스 산출물로 잡아둔
  "OSS 기여 1건" 의 후보다. *(TODO)*
- **교훈** — 업스트림 공식 파일도 외부 레지스트리 변경에는 무력하다. "공식이니까 된다"는
  가정이 첫 번째로 깨진 지점이었다.

## 막힌 기록

> 해결되지 않은 것, 우회한 것, 나중의 나를 위한 메모. 비어 있으면 아직 아무것도
> 진지하게 시도하지 않았다는 뜻이다.

- **2026-09-15 · minio 이미지 pull 실패** — 에러 메시지가 `docker login` 을 가리켜
  인증 문제로 오인하기 쉬웠다. 실제 원인은 Docker Hub 에서 저장소가 사라진 것.
  **레지스트리 API 를 직접 조회해서 확인**했고, 메시지를 그대로 믿지 않은 것이 주효했다.
  → D-008 로 해결.
- **미해결 · 컨테이너 내부 랜덤 I/O 미측정** — 외장 볼륨 순차 수치(D-004)만 있다.
  DiskANN·mmap 벤치의 신뢰도가 여기 달려 있으므로 W2 전에 측정해야 한다.
